{# pass last segment as slug to api - to keep pretty urls #}
{#{%  set shipRoot = 'health-well-being/seafarers-health-resources' %}#}

{# deal with sit ehandle name that disallows hypehns but has a hypen in url base#}
{%  if currentSite.handle == "kene" %}
		{% set shipRootUrl =  "k-ene/resources" | t %}
	{% else %}
		{% set shipRootUrl = currentSite.handle ~ "/resources" | t %}
{% endif %}
{% js %}
	var baseNavLink = 'https://' + window.location.hostname;
	var shipRootUrl = '{{ shipRootUrl }}';
	var lastSegment = '{{craft.app.request.segments|last}}';
	var firstSegment = '{{craft.app.request.getSegment(1)}}';
	var catSegment = '{{craft.app.request.getSegment(2)}}';
	var catRoot = '{{ shipRootUrl }}';
	var catUrl =  '/{{ shipRootUrl }}/';
{#	var catUrl =  '/{{ craft.app.sites.currentSite }}/{{ shipRootUrl }}/';#}
{#	//var domain = '{{ craft.config.shipAPIdomain }}';#}
	var domain = '{{ craft.app.config.general.shipAPIdomain }}';
	var locale = '{{'en'}}';
{#	console.log("shipRoot = " + shipRoot);#}
	console.log("shipRootUrl = " + shipRootUrl);
	console.log("basenavlink = " + baseNavLink);
	console.log("firstSegment = " + firstSegment);
	console.log("lastseg = " + lastSegment);
	console.log("catSegment = " + catSegment);
	console.log("catRoot = " + shipRootUrl);
	console.log("catUrl = " + shipRootUrl);
{%  endjs %}


